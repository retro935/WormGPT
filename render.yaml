services:
  - type: docker
    name: wormgpt-bot
    env: docker
    plan: free
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: MODEL_API_KEY
        sync: false
      - key: HF_TOKEN
        sync: false
      - key: LOG_CHANNEL_ID
        sync: false
