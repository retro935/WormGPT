python-telegram-bot==20.7
requests==2.31.0
Pillow==10.1.0  # Fix para PIL
langdetect==1.0.9  # Si lo necesitas; si falla, quítalo (código lo maneja optional)
setuptools==70.0.0  # Fix para metadata '__version__'
wheel==0.43.0  # Pa' builds suaves
